import { withComponent } from '@primereact/core/component';
import { style } from '@primereact/styles/inputgroupaddon';
import { defaultProps } from './InputGroupAddon.props';

export const useInputGroupAddon = withComponent(
    () => {
        return {};
    },
    defaultProps,
    style
);
